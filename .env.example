# BankRegPulse API URL (optional, defaults to production)
# BANKREGPULSE_API_URL=https://bankregpulse-enterprise-api.onrender.com

# Transport mode: 'stdio' (default) or 'http'
# MCP_TRANSPORT=stdio

# HTTP server port (only used when MCP_TRANSPORT=http)
# PORT=3000
